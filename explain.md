# RMCS Slave 项目介绍

**RMCS (RoboMaster Control System)** 是一个STM32F407嵌入式固件，用于机器人控制系统下位机。

## 核心功能
- **USB数据转发**: 通过USB 2.0 FS端口，以BULK模式将CAN/UART/SPI等接口数据实时转发至上位机
- **无操作系统**: 裸机运行，最大化转发效率
- **无锁同步**: 使用CAS原子操作保证中断安全

## 关键模块

| 模块 | 文件 | 说明 |
|------|------|------|
| **USB CDC** | `app/usb/cdc.cpp/hpp` | 主通信接口，数据分发中心 |
| **CAN总线** | `app/can/can.cpp/hpp` | 2路CAN(1Mbps)，支持标准/扩展帧 |
| **UART串口** | `app/uart/uart.cpp/hpp` | 3路UART，双缓冲机制 |
| **BMI088 IMU** | `app/spi/bmi088/` | 加速度计(1600Hz) + 陀螺仪(2000Hz) |
| **蜂鸣器** | `app/buzzer/buzzer.hpp` | 4音符旋律播放 |
| **LED指示灯** | `app/led/led.hpp` | RGB状态指示 |

## 数据流

```
┌─────────────┐         ┌─────────────┐
│  上位机PC   │←──USB──→│  STM32F407  │
└─────────────┘         └──────┬──────┘
                               │
           ┌───────────────────┼───────────────────┐
           │                   │                   │
      ┌────▼────┐        ┌────▼────┐        ┌────▼────┐
      │  CAN1   │        │  UART   │        │  BMI088 │
      │  CAN2   │        │ DBUS    │        │  IMU    │
      └─────────┘        └─────────┘        └─────────┘
```

## 关键设计模式
- **Lazy延迟初始化**: 解决全局对象初始化顺序问题
- **无锁环形缓冲区**: 中断和主循环之间的安全数据传输
- **双缓冲**: UART发送的无锁切换
- **WCID免驱**: Windows系统自动识别为WinUSB设备

版本号: `2.1.2`，支持RoboMaster C型开发板。
